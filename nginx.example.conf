# Nginx Konfigurationsbeispiel für movie-bite.suppenstudios.work

server {
    listen 80;
    server_name movie-bite.suppenstudios.work;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    server_name movie-bite.suppenstudios.work;

    # SSL Zertifikate (z.B. Let's Encrypt / Cloudflare Origin)
    # ssl_certificate /etc/letsencrypt/live/movie-bite.suppenstudios.work/fullchain.pem;
    # ssl_certificate_key /etc/letsencrypt/live/movie-bite.suppenstudios.work/privkey.pem;

    # Frontend & API
    location / {
        proxy_pass http://127.0.0.1:3001;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # WICHTIG: WebSocket / Socket.IO Weiterleitung
    location /socket.io/ {
        proxy_pass http://127.0.0.1:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 86400;
    }
}
